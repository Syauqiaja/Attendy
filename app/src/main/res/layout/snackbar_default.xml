<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/transparent">

    <RelativeLayout
        android:id="@+id/button_click_parent"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/white"
        android:clickable="true"
        android:focusable="true">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/iv_bengkelin_logo"
            android:layout_width="30dp"
            android:layout_marginHorizontal="@dimen/_8sdp"
            android:layout_height="match_parent"
            android:src="@drawable/attendy_icon"
            app:tint="@color/primary"/>

        <TextView
            android:id="@+id/toast_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginStart="4dp"
            android:ellipsize="end"
            android:layout_toEndOf="@id/iv_bengkelin_logo"
            android:layout_alignParentEnd="true"
            android:maxLines="2"
            tools:text="@tools:sample/lorem[3]"
            android:textAppearance="@style/AppTheme.TextAppearance.Body.L.Medium"/>

    </RelativeLayout>


</RelativeLayout>
