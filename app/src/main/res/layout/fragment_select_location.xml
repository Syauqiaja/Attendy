<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".presentation.main.location.SelectLocationFragment"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:navigationIcon="@drawable/ic_back_arrow"
        app:title="Select Location"
        app:layout_constraintTop_toTopOf="parent"
        app:titleTextAppearance="@style/AppTheme.TextAppearance.Body.XL"/>
    <TextView
        android:id="@+id/tv_select_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/select_location_below"
        android:layout_marginTop="@dimen/_16sdp"
        android:textAppearance="@style/AppTheme.TextAppearance.Body.L.SemiBold"
        android:layout_marginHorizontal="@dimen/_24cdp"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_location"
        app:layout_constraintTop_toBottomOf="@id/tv_select_location"
        android:layout_marginHorizontal="@dimen/_24cdp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:itemCount="4"/>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/fab_create_new"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="@dimen/_24cdp"
        android:layout_marginBottom="@dimen/_40cdp"
        app:cardElevation="@dimen/_3sdp"
        app:cardUseCompatPadding="true"
        app:strokeWidth="2dp"
        app:strokeColor="@color/primary">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@color/white"
            android:gravity="center_vertical"
            android:paddingHorizontal="@dimen/_16sdp"
            android:paddingVertical="@dimen/_12sdp">
            <ImageView
                android:layout_width="@dimen/_24cdp"
                android:layout_height="@dimen/_24cdp"
                android:src="@drawable/ic_add"
                app:tint="@color/primary"
                android:layout_marginEnd="@dimen/_4sdp"/>
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/create_new"
                android:includeFontPadding="false"
                android:textColor="@color/primary"
                android:textAppearance="@style/AppTheme.TextAppearance.Body.L"/>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</androidx.constraintlayout.widget.ConstraintLayout>